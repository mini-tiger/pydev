# env
python3.8
```shell
apt-get update
apt-get install python3.8-dev
apt-get install build-essential libssl-dev libffi-dev python-dev
apt-get install libpcap-dev libpq-dev
apt-get install -y libmagic-dev
```

# venv

# pip
```shell
// ocr cpu
pip install paddlepaddle -i https://mirror.baidu.com/pypi/simple
pip install "paddleocr>=2.0.1"
pip install fitz
pip install frontend


pip3 install wheel
pip3 install coincurve
pip install ta-lib
pip uninstall numpy
pip install numpy
pip install "unstructured[all-docs]"

pip uninstall python-libmagic
pip uninstall python-magic 
pip install python-magic==0.4.15

pip uninstall pymupdf
pip install pymupdf==1.19.6
```
ocr 模型
```shell
https://github.com/PaddlePaddle/PaddleOCR/blob/static/doc/doc_ch/models_list.md
```